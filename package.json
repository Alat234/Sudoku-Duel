{
  "name": "sudoku-duel-mvc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}